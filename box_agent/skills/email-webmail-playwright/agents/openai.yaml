interface:
  display_name: "Webmail Playwright"
  short_description: "Use browser automation for generic email tasks."
  default_prompt: "Use $email-webmail-playwright to open webmail, compose the email, and send only when explicitly authorized."

dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Browser automation for webmail UI fallback."

policy:
  allow_implicit_invocation: true
